
$(function () {
    $('.backbar').on('touchmove', function (event) {event.preventDefault();});    //把header的滑动给禁掉

    // $('footer').on('touchmove', function (event) {event.preventDefault();});    //把footer的滑动给禁掉
    
    // //把nav的默认滚动去掉
	// $('.tab').on('touchmove', function (event) {event.preventDefault();});	
    
    // /*退出提示框*/
    // $('.weui-mask').on('touchmove', function (event) {event.preventDefault();});   
    // $('.weui-dialog').on('touchmove', function (event) {event.preventDefault();});
    
    // /*数据加载中提示*/
    // $('.weui-mask_transparent').on('touchmove', function (event) {event.preventDefault();});   
    // $('.weui-toast').on('touchmove', function (event) {event.preventDefault();});
    
    // $('.add').on('touchmove', function (event) {event.preventDefault();});
});


